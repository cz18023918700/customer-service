# Nginx 配置（备案通过后启用）
# 用法：
#   1. 把 YOUR_DOMAIN 替换成你的域名
#   2. 复制到服务器 /etc/nginx/sites-available/customer-service
#   3. ln -s /etc/nginx/sites-available/customer-service /etc/nginx/sites-enabled/
#   4. sudo certbot --nginx -d YOUR_DOMAIN（自动配SSL）
#   5. sudo nginx -t && sudo systemctl reload nginx

server {
    listen 80;
    server_name YOUR_DOMAIN;

    # 企微回调验证需要 80 端口
    location / {
        proxy_pass http://127.0.0.1:8900;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE 流式输出支持
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
    }

    # 图片上传大小限制
    client_max_body_size 10M;
}

# HTTPS 配置（certbot 会自动生成，这里是模板参考）
# server {
#     listen 443 ssl;
#     server_name YOUR_DOMAIN;
#
#     ssl_certificate /etc/letsencrypt/live/YOUR_DOMAIN/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/YOUR_DOMAIN/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8900;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_buffering off;
#         proxy_read_timeout 300s;
#     }
#
#     client_max_body_size 10M;
# }
